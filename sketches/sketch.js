function setup() {
  // create a canvas
  createCanvas(600, 800);
}

function draw() {
  // colour the background black
  background(0);
  // select white as a colour
  fill(255);
  // draw a rectangle
  rect(150, 150, 100, 100);
}
